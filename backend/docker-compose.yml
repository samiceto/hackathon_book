version: '3.8'

services:
  backend:
    build: .
    container_name: rag-chatbot-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./src:/app/src
    environment:
      - PYTHONUNBUFFERED=1
