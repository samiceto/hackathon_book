# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.12.3,<3.0.0

# Vector Database
qdrant-client==1.7.3

# Embeddings & LLM
cohere==4.47
openai>=2.9.0,<3.0.0
google-generativeai==0.3.2

# OpenAI Agents SDK
agents>=0.2.0
git+https://github.com/openai/chatkit-python.git@v1.4.0

# Web Scraping (only needed for ingestion - not for deployed backend)
# beautifulsoup4==4.12.3
# lxml==5.1.0
# requests==2.31.0

# Text Processing (only needed for ingestion - not for deployed backend)
# langchain-text-splitters==0.0.1

# Environment Variables
python-dotenv==1.0.1

# Rate Limiting
slowapi==0.1.9

# CORS
# (included in fastapi, no separate install needed)

# Testing (optional)
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
